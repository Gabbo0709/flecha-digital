<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Flecha_Digital.View.MainPage"
             Title="Main">
    <Grid ColumnDefinitions="*,*,*,*"
          ColumnSpacing="0"
          RowDefinitions="*,50"
          RowSpacing="0">
        <CollectionView>

        </CollectionView>
        <Button Text="Menu"
                Grid.Column="0"/>
            
            <Button Text="Actividad"   
                    Grid.Column="1"
                    Grid.Row="1"
        BackgroundColor="{StaticResource BotonAzul}"/>
            <Button Text="Usuario"
                    Grid.Row="1"
                    Grid.Column="2"

        BackgroundColor="{StaticResource CelestePrincipal}"/>
        <Button Text="Contacto"
                Grid.Column="3"
                Grid.Row="1"
        
        BackgroundColor="{StaticResource CelestePrincipal}"/>


    </Grid>
</ContentPage>
