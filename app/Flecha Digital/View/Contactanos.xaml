<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Flecha_Digital.View.Contactanos"
             xmlns:viewmodel="clr-namespace:Flecha_Digital.ViewModel"
             x:DataType="viewmodel:InicioViewModel"
             xmlns:model="clr-namespace:Flecha_Digital.Model"
             Title="Contactanos">
    <Grid ColumnDefinitions="*"
          RowDefinitions="*,Auto,*">
        <CollectionView Grid.ColumnSpan="0"
                        Grid.Row="0">
            <CollectionView.Header>
                <StackLayout>
                    <Label Text="Contactanos"
                           FontSize="Large"
                           HorizontalOptions="Center"/>
                </StackLayout>
            </CollectionView.Header>
        </CollectionView>
        <VerticalStackLayout Grid.Row="1">
            <Label Text="Correo: "/>
            <Label Text="Número de telefono"/>
        </VerticalStackLayout>
        <VerticalStackLayout Grid.Row="2">
            <Image Source="Facebook"/>
            <Image Source="Instagram"/>
        </VerticalStackLayout>
    </Grid>
    
</ContentPage>